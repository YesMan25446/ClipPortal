# SMTP and Magic Link configuration

Use these environment variables (set them in Railway; for local dev, copy to a new .env file and fill in real values).

# Public site URL used to build magic links
SITE_BASE_URL=

# SMTP server
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=no-reply@example.com

# Security (recommended)
JWT_SECRET=
EMAIL_ENC_KEY=
# EMAIL_ENC_KEY should be 64 hex characters (32 bytes), e.g. from: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
